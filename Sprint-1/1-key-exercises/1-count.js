let count = 0;

count = count + 1;

console.log(count); // Output: 1
// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing
//Line 3 is reassigning the count variable to a new value which is the current value increment by 1, "=" is assigning operator that assigning value of right side which mens count increment 1 to the left side value of count variable
